{
  "name": "mesto",
  "version": "1.0.0",
  "description": "Интерактивная страница для просмотра и добавления фотографий мест",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^5.0.0"
  }
}